create database mamda;
use mamda;

create table personne (
  id  int(11) auto_increment primary key,
  name varchar(30) not null,
  email varchar(30) not null,
  email varchar(30) not null,
  birthdate varchar(30) not null,
  city varchar(30) not null
);